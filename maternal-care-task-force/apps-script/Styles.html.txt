<!-- FILE: Styles.html — inlined by include('Styles') in Index.html -->
<style>
  /* ------------------------------------------------------------------
     Palette. Deep navy carries the institutional weight; a single teal
     accent carries every interactive affordance. Semantic red and green
     appear only where an indicator is compared with the state average,
     so colour always means something.
     ------------------------------------------------------------------ */
  :root {
    --navy-900: #131e30;
    --navy-800: #1b2b45;
    --navy-700: #24395a;
    --navy-100: #eef1f6;
    --navy-050: #f6f8fb;

    --accent: #0f6b83;
    --accent-dark: #0b5163;
    --accent-050: #e8f1f4;

    --pos: #1c6440;
    --pos-050: #e9f2ec;
    --neg: #9a2f27;
    --neg-050: #f8ecea;
    --warn: #85560f;
    --warn-050: #f8f1e4;

    --ink: #131824;
    --ink-2: #333d4d;
    --muted: #5c6674;
    --line: #dce1e9;
    --line-strong: #c3ccd8;
    --bg: #f4f6f9;
    --card: #ffffff;

    --radius: 8px;
    --radius-lg: 12px;
    --shadow-sm: 0 1px 2px rgba(19, 30, 48, .06);
    --shadow: 0 1px 2px rgba(19, 30, 48, .05), 0 6px 16px -6px rgba(19, 30, 48, .12);
    --ease: cubic-bezier(.16, 1, .3, 1);
  }

  *, *::before, *::after { box-sizing: border-box; }
  html { -webkit-text-size-adjust: 100%; }
  html, body { margin: 0; padding: 0; }
  body {
    font: 15px/1.6 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    color: var(--ink);
    background: var(--bg);
    font-variant-numeric: tabular-nums;
  }

  /* Browser surfaces themed from the palette rather than left to defaults. */
  ::selection { background: #c8dde4; color: var(--navy-900); }
  :focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; border-radius: 3px; }
  .hero :focus-visible { outline-color: #9fd4e2; }
  * { scrollbar-width: thin; scrollbar-color: var(--line-strong) transparent; }
  *::-webkit-scrollbar { width: 10px; height: 10px; }
  *::-webkit-scrollbar-thumb { background: var(--line-strong); border-radius: 6px; border: 3px solid var(--bg); }
  *::-webkit-scrollbar-thumb:hover { background: #a9b4c3; }
  a { color: var(--accent-dark); text-underline-offset: 2px; text-decoration-thickness: 1px; }
  a:hover { color: var(--accent); }

  .wrap { max-width: 1080px; margin: 0 auto; padding: 0 24px; }
  .skip { position: absolute; left: -999px; top: 0; background: #fff; padding: 8px; }
  .skip:focus { left: 8px; top: 8px; z-index: 20; }

  /* ---------------- header ---------------- */
  .hero { background: linear-gradient(178deg, var(--navy-800), var(--navy-900)); color: #fff; padding: 34px 0 24px; }
  .hero h1 { margin: 0; font-size: clamp(26px, 3.4vw, 36px); line-height: 1.12; font-weight: 700; letter-spacing: -.02em; }
  .subtitle { margin: 6px 0 0; font-size: 15px; font-weight: 600; color: #a9bdd6; letter-spacing: .01em; }
  .tagline { margin: 14px 0 0; max-width: 62ch; font-size: 15px; color: #d4deeb; }
  .announce { margin-top: 18px; background: rgba(255,255,255,.07); border: 1px solid rgba(169,189,214,.32); border-radius: var(--radius); padding: 11px 14px; font-size: 14px; color: #e4ebf3; }
  .announce a { color: #9fd4e2; }

  /* indicator tiles */
  .indicators { display: grid; gap: 8px; grid-template-columns: repeat(auto-fit, minmax(152px, 1fr)); margin: 20px 0 0; }
  .ind {
    display: block; width: 100%; text-align: left; font: inherit; cursor: pointer;
    background: rgba(255,255,255,.06); border: 1px solid rgba(169,189,214,.28);
    border-radius: var(--radius); padding: 10px 12px; color: #d4deeb;
    transition: background .18s var(--ease), border-color .18s var(--ease);
  }
  .ind:hover { background: rgba(255,255,255,.12); border-color: rgba(169,189,214,.5); }
  .ind .n { display: block; font-size: 12px; line-height: 1.3; min-height: 31px; color: #a9bdd6; }
  .ind .v { display: block; font-size: 23px; font-weight: 700; margin-top: 5px; letter-spacing: -.02em; color: #fff; }
  .ind .c { display: block; font-size: 11px; margin-top: 2px; color: #93a8c2; }
  .ind.worse .v { color: #f0a79e; }
  .ind.better .v { color: #93d3ac; }
  .indicator-source { margin: 10px 0 0; font-size: 12px; color: #93a8c2; }
  .indicator-source a { color: #9fd4e2; }

  /* ---------------- nav ---------------- */
  .tabs { position: sticky; top: 0; z-index: 8; background: rgba(255,255,255,.94); backdrop-filter: saturate(1.6) blur(8px); border-bottom: 1px solid var(--line); }
  .tabs-inner { display: flex; gap: 2px; overflow-x: auto; padding: 7px 24px; }
  .tab { flex: 0 0 auto; border: 0; background: transparent; padding: 8px 13px; border-radius: 6px; font: inherit; font-size: 14px; font-weight: 600; color: var(--muted); cursor: pointer; white-space: nowrap; transition: background .16s var(--ease), color .16s var(--ease); }
  .tab:hover { background: var(--navy-100); color: var(--navy-800); }
  .tab[aria-current="page"] { background: var(--navy-800); color: #fff; }

  main { padding: 30px 24px 48px; }
  .status { padding: 13px 16px; border-radius: var(--radius); background: var(--navy-100); color: var(--navy-800); font-weight: 600; }
  .status.error { background: var(--neg-050); color: var(--neg); }

  h2 { font-size: 24px; margin: 0 0 6px; color: var(--navy-800); letter-spacing: -.02em; font-weight: 700; }
  h3 { font-size: 17px; margin: 0 0 6px; letter-spacing: -.01em; }
  h4 { font-size: 15px; margin: 0 0 4px; }
  p { margin: 0 0 10px; }
  .lead { color: var(--muted); margin: 0 0 22px; max-width: 68ch; }
  .section { margin-bottom: 30px; }
  .section > h3 { margin-bottom: 10px; padding-top: 6px; }
  .muted { color: var(--muted); }
  .small { font-size: 13px; }

  /* ---------------- disclosure ---------------- */
  .disc { background: var(--card); border: 1px solid var(--line); border-radius: var(--radius); margin-bottom: 8px; box-shadow: var(--shadow-sm); }
  .disc + .section { margin-top: 26px; }
  .disc > summary {
    display: flex; align-items: flex-start; gap: 12px; cursor: pointer;
    padding: 13px 15px; list-style: none; border-radius: var(--radius);
    transition: background .16s var(--ease);
  }
  .disc > summary::-webkit-details-marker { display: none; }
  .disc > summary:hover { background: var(--navy-050); }
  .disc[open] > summary { border-bottom: 1px solid var(--line); border-radius: var(--radius) var(--radius) 0 0; }
  .disc .chev { flex: 0 0 auto; width: 16px; height: 16px; margin-top: 3px; color: var(--muted); transition: transform .22s var(--ease); }
  .disc[open] > summary .chev { transform: rotate(90deg); }
  .disc .sum { flex: 1 1 auto; min-width: 0; }
  .disc .sum-title { font-weight: 650; color: var(--ink); display: block; }
  .disc .sum-meta { display: block; margin-top: 3px; font-size: 13px; color: var(--muted); }
  .disc .sum-tags { margin-top: 6px; }
  .disc-body { padding: 14px 15px 16px 43px; }
  .disc-body > p, .disc-body > h4, .disc-body > .implication, .disc-body > .notice { max-width: 74ch; }
  .disc-body > :last-child { margin-bottom: 0; }
  @keyframes reveal { from { opacity: 0; transform: translateY(-3px); } to { opacity: 1; transform: none; } }
  .disc[open] > .disc-body { animation: reveal .26s var(--ease); }
  @media (prefers-reduced-motion: reduce) { .disc[open] > .disc-body { animation: none; } .disc .chev { transition: none; } }

  /* Nested one level: a driver inside its indicator, a program inside its
     category. Hairline-separated rows rather than a card within a card. */
  .disc .disc { box-shadow: none; border: 0; border-top: 1px solid var(--line); border-radius: 0; margin: 0; background: transparent; }
  .disc .disc:first-of-type { border-top: 0; }
  .disc .disc > summary { padding: 11px 4px; border-radius: 0; }
  .disc .disc[open] > summary { border-bottom: 0; }
  .disc .disc > .disc-body { padding: 0 4px 14px 32px; }

  .acc-bar { display: flex; align-items: center; gap: 8px; margin: 0 0 12px; }
  .acc-bar button { border: 1px solid var(--line-strong); background: var(--card); color: var(--ink-2); font: inherit; font-size: 13px; font-weight: 600; padding: 5px 11px; border-radius: 6px; cursor: pointer; transition: background .16s var(--ease), border-color .16s var(--ease); }
  .acc-bar button:hover { background: var(--navy-050); border-color: var(--muted); }
  .acc-bar .count { margin-left: auto; font-size: 13px; color: var(--muted); }

  /* ---------------- cards, used sparingly ---------------- */
  .card { background: var(--card); border: 1px solid var(--line); border-radius: var(--radius); padding: 15px; box-shadow: var(--shadow-sm); margin-bottom: 8px; }
  .card p { margin: 6px 0; }
  .meta { color: var(--muted); font-size: 13px; }
  .grid { display: grid; gap: 10px; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); }

  .pill { display: inline-block; font-size: 11.5px; font-weight: 650; padding: 2px 8px; border-radius: 4px; background: var(--navy-100); color: var(--navy-700); margin: 0 5px 4px 0; letter-spacing: .01em; }
  .pill.cecil { background: var(--warn-050); color: var(--warn); }
  .pill.harford { background: var(--accent-050); color: var(--accent-dark); }
  .pill.both { background: var(--pos-050); color: var(--pos); }
  .pill.ind-tag { background: transparent; color: var(--muted); border: 1px solid var(--line-strong); font-weight: 600; }
  .pill.status-open, .pill.status-active, .pill.status-awarded { background: var(--pos-050); color: var(--pos); }
  .pill.status-pending, .pill.status-applied, .pill.status-forming { background: var(--warn-050); color: var(--warn); }
  .pill.status-closed, .pill.status-done, .pill.status-notapplied { background: var(--navy-100); color: var(--muted); }

  /* ---------------- table ---------------- */
  .table-scroll { overflow-x: auto; border: 1px solid var(--line); border-radius: var(--radius); background: var(--card); }
  table { width: 100%; border-collapse: collapse; }
  th, td { padding: 10px 13px; text-align: left; border-bottom: 1px solid var(--line); vertical-align: top; font-size: 14px; }
  tbody tr:last-child td { border-bottom: 0; }
  tbody tr:hover { background: var(--navy-050); }
  th { background: var(--navy-050); color: var(--navy-700); font-size: 11.5px; text-transform: uppercase; letter-spacing: .05em; font-weight: 700; border-bottom: 1px solid var(--line-strong); }
  td.num { text-align: right; font-variant-numeric: tabular-nums; white-space: nowrap; }
  td.worse { color: var(--neg); font-weight: 700; }
  td.better { color: var(--pos); font-weight: 700; }
  .legend { font-size: 13px; color: var(--muted); margin-top: 10px; }

  /* ---------------- filters ---------------- */
  .filters { display: flex; flex-wrap: wrap; gap: 10px; margin: 0 0 16px; align-items: center; }
  .filters label { font-size: 13px; font-weight: 600; color: var(--muted); display: inline-flex; align-items: center; gap: 6px; }
  .filters select, .filters input { font: inherit; font-size: 14px; padding: 6px 9px; border: 1px solid var(--line-strong); border-radius: 6px; background: var(--card); color: var(--ink); min-width: 150px; }
  .filters select:hover, .filters input:hover { border-color: var(--muted); }
  .count { font-size: 13px; color: var(--muted); margin-left: auto; }

  /* ---------------- evidence ---------------- */
  .cols { display: grid; gap: 10px; grid-template-columns: repeat(auto-fit, minmax(190px, 1fr)); margin: 10px 0; }
  .cols > div { background: var(--navy-050); border: 1px solid var(--line); border-radius: var(--radius); padding: 10px 12px; font-size: 13.5px; }
  .cols strong { display: block; font-size: 11px; text-transform: uppercase; letter-spacing: .05em; color: var(--navy-700); margin-bottom: 4px; }
  .cite { display: block; margin-top: 7px; font-size: 12px; }
  .cite.muted { color: var(--muted); font-style: italic; }
  .implication { background: var(--accent-050); border: 1px solid #cfe3e9; border-radius: var(--radius); padding: 10px 12px; margin-top: 10px; font-size: 14px; }
  .result { color: var(--pos); font-weight: 650; }

  /* ---------------- bars ---------------- */
  .bar { display: flex; align-items: center; gap: 10px; margin: 5px 0; font-size: 13.5px; }
  .bar .track { flex: 1; background: var(--navy-100); border-radius: 3px; height: 8px; overflow: hidden; }
  .bar .fill { background: var(--navy-700); height: 100%; border-radius: 3px; }
  .bar .lbl { min-width: 210px; }
  .bar .n { min-width: 26px; text-align: right; font-weight: 700; font-variant-numeric: tabular-nums; }

  .btn { display: inline-block; background: var(--accent-dark); color: #fff !important; text-decoration: none; padding: 9px 15px; border-radius: 6px; font-weight: 650; font-size: 14px; transition: background .16s var(--ease); }
  .btn:hover { background: var(--accent); color: #fff !important; }
  .btn.secondary { background: var(--card); color: var(--accent-dark) !important; border: 1px solid var(--line-strong); }
  .btn.secondary:hover { background: var(--navy-050); color: var(--accent-dark) !important; }

  .notice { border: 1px solid #e6d5ae; background: var(--warn-050); color: #6b4712; border-radius: var(--radius); padding: 11px 13px; font-size: 13.5px; }

  .foot { padding: 22px 24px 44px; border-top: 1px solid var(--line); font-size: 13px; color: var(--muted); max-width: 78ch; }
  .foot p { margin: 0 0 9px; }

  @media (max-width: 640px) {
    .wrap { padding: 0 16px; }
    main { padding: 22px 16px 36px; }
    .tabs-inner { padding: 7px 16px; }
    .hero { padding: 22px 0 16px; }
    .tagline { margin-top: 10px; font-size: 14px; }
    .indicators { gap: 6px; margin-top: 14px; grid-template-columns: repeat(2, 1fr); }
    .ind { padding: 8px 10px; }
    .ind .n { font-size: 11.5px; min-height: 28px; }
    .ind .v { font-size: 19px; margin-top: 3px; }
    .ind .c { font-size: 10.5px; line-height: 1.35; }
    .indicator-source { margin-top: 8px; }
    .disc-body { padding: 13px 14px 15px; }
    .disc .disc-body .disc-body { padding-left: 14px; }
    th, td { font-size: 13px; padding: 8px 10px; }
    .bar .lbl { min-width: 130px; }
    .count { margin-left: 0; width: 100%; }
  }
</style>
